// Auth disabled — all requests are unauthenticated.
// API key support can be re-enabled here when needed.
export const getApiKey = (): string => "";
export const getAuthHeaders = (): Record<string, string> => ({});
